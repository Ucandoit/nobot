version: '3'
services:
  nobot-redis:
    image: redis
  nobot-database:
    image: postgres
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_DB: nobot
      POSTGRES_USER: nobot
      POSTGRES_PASSWORD: nobot

    volumes:
      - nobot-pgdata:/var/lib/postgresql/data/pgdata
    ports:
      - '5433:5432'
  nobot-token-manager:
    build:
      context: ./
      dockerfile: ./packages/tools/token-manager/Dockerfile.dev
    volumes:
      - /usr/src/app/node_modules
      - /usr/src/app/packages/core/database/node_modules
      - /usr/src/app/packages/core/commons/node_modules
      - /usr/src/app/packages/tools/token-manager/node_modules
      - .:/usr/src/app
  auction-sniping:
    build:
      context: ./
      dockerfile: ./packages/api/auction-sniping/Dockerfile.dev
    volumes:
      - /usr/src/app/node_modules
      - /usr/src/app/packages/core/database/node_modules
      - /usr/src/app/packages/core/commons/node_modules
      - /usr/src/app/packages/api/auction-sniping/node_modules
      - .:/usr/src/app
    ports:
      - '4001:3000'
volumes:
    nobot-pgdata:
