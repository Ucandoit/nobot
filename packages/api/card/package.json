{
  "name": "@nobot-api/card",
  "version": "1.0.0",
  "private": true,
  "description": "Nobot card module",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "rimraf lib && tsc -b --force ./tsconfig.build.json",
    "watch": "rimraf lib && tsc -b ./tsconfig.build.json --watch",
    "dev": "nodemon",
    "start": "node ./lib/index.js"
  },
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "@nobot-core/commons": "0.1.0",
    "@nobot-core/database": "0.1.0",
    "express": "^4.17.1",
    "inversify": "^5.0.1",
    "log4js": "^6.2.1",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.2.24"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "nodemon": "^2.0.3"
  }
}
