const METADATA_KEY = {
  /**
   * defines the identifier to be binded using inversify
   */
  AUTOWIRED: 'nobot:autowired',
  /**
   * defines the base path of a controller
   */
  BASE_PATH: 'nobot:basePath',
  /**
   * defines the routes of a controller
   */
  ROUTES: 'nobot:routes'
};

export default METADATA_KEY;
